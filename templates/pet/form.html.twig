{% extends 'front/base.html.twig' %}

{% block title %}
    {{ is_edit ? 'Modifier un animal' : 'Ajouter un animal' }}
{% endblock %}


{% block body %}
<section class="content">
    <div class="pet-form-card">

        <div class="form-header">
<h2>
    {{ is_edit ? 'Modifier un animal' : 'Ajouter un animal' }}
</h2>
            <a href="{{ path('app_pet') }}" class="back-link">← Retour</a>
        </div>

        {{ form_start(form) }}

        <div class="form-grid">

            <!-- LEFT COLUMN -->
            <div class="left-column">

                <div class="form-group">
                    <label>Nom</label>
                    {{ form_widget(form.name, {'attr': {'class': 'input-modern'}}) }}
                </div>

                <div class="form-group">
                    <label>Race</label>
                    {{ form_widget(form.breed, {'attr': {'class': 'input-modern'}}) }}
                </div>

                <div class="form-group">
                    <label>Poids (kg)</label>
                    {{ form_widget(form.weight, {'attr': {'class': 'input-modern'}}) }}
                </div>

                <div class="form-group">
                    <label>Date de naissance</label>
                    {{ form_widget(form.birthDate, {'attr': {'class': 'input-modern'}}) }}
                </div>

            </div>

            <!-- RIGHT COLUMN -->
            <div class="right-column">

                <label class="section-label">Type d'animal</label>
                <div class="type-grid">
                    {% for child in form.typePet %}
                        <label class="type-card">
                            {{ form_widget(child) }}
                            <span>{{ child.vars.label }}</span>
                        </label>
                    {% endfor %}
                </div>

                <label class="section-label">Genre</label>
                <div class="gender-toggle">
                    {% for child in form.gender %}
                        <label class="gender-btn">
                            {{ form_widget(child) }}
                            {{ child.vars.label }}
                        </label>
                    {% endfor %}
                </div>

            </div>

        </div>

        <!-- DESCRIPTION FULL WIDTH -->
        <div class="form-group full-width">
            <label>Description</label>
            {{ form_widget(form.description, {'attr': {'class': 'textarea-modern'}}) }}
        </div>

        <!-- HEALTH SECTION -->
        <div class="health-section">
            <h3>État de santé</h3>

            <div class="health-grid">
                <label class="health-card">
                    {{ form_widget(form.isVacinated) }}
                    Vacciné
                </label>

                <label class="health-card">
                    {{ form_widget(form.hasContagiousDisease) }}
                    Maladie contagieuse
                </label>

                <label class="health-card">
                    {{ form_widget(form.hasMedicalRecord) }}
                    Dossier médical
                </label>

                <label class="health-card">
                    {{ form_widget(form.hasCriticalCondition) }}
                    État critique
                </label>
            </div>
        </div>

        <div class="submit-area">
<button class="btn-modern">
    {{ is_edit ? 'Mettre à jour' : 'Enregistrer' }}
</button>
        </div>

        {{ form_end(form) }}

    </div>
</section>
{% endblock %}
