<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin - SitMyPet{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&family=Poppins:wght@600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --sidebar-width: 260px;
            --primary: #4f7942;
            --primary-dark: #2d5a27;
            --primary-light: #e8f5e9;
            --accent: #f4a261;
            --danger: #e53935;
            --sidebar-bg: #1a2e1a;
            --sidebar-text: rgba(255,255,255,0.75);
            --sidebar-active: rgba(255,255,255,0.12);
        }
        * { box-sizing: border-box; }
        body {
            font-family: 'Nunito', sans-serif;
            background: #f4f6f8;
            margin: 0;
        }

        /* ===== SIDEBAR ===== */
        .sidebar {
            position: fixed;
            top: 0; left: 0;
            width: var(--sidebar-width);
            height: 100vh;
            background: var(--sidebar-bg);
            display: flex;
            flex-direction: column;
            z-index: 1000;
            overflow-y: auto;
        }
        .sidebar-brand {
            padding: 24px 20px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.08);
        }
        .sidebar-brand .brand-name {
            font-family: 'Poppins', sans-serif;
            font-size: 1.2rem;
            font-weight: 700;
            color: white;
        }
        .sidebar-brand .brand-sub {
            font-size: 0.72rem;
            color: rgba(255,255,255,0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .sidebar-section {
            padding: 18px 14px 6px;
            font-size: 0.65rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: rgba(255,255,255,0.3);
        }
        .sidebar-nav { padding: 0 10px; flex: 1; }
        .sidebar-nav .nav-link {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 11px 14px;
            border-radius: 10px;
            color: var(--sidebar-text);
            text-decoration: none;
            font-size: 0.88rem;
            font-weight: 600;
            margin-bottom: 3px;
            transition: all 0.2s;
        }
        .sidebar-nav .nav-link:hover {
            background: var(--sidebar-active);
            color: white;
        }
        .sidebar-nav .nav-link.active {
            background: var(--primary);
            color: white;
            box-shadow: 0 4px 12px rgba(79,121,66,0.4);
        }
        .sidebar-nav .nav-link i {
            width: 18px;
            text-align: center;
            font-size: 0.9rem;
        }
        .sidebar-nav .nav-link .badge-count {
            margin-left: auto;
            background: rgba(255,255,255,0.15);
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 0.7rem;
        }
        .sidebar-footer {
            padding: 16px 14px;
            border-top: 1px solid rgba(255,255,255,0.08);
        }
        .sidebar-footer .admin-info {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }
        .sidebar-footer .admin-avatar {
            width: 36px; height: 36px;
            background: var(--primary);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            font-size: 1rem;
            color: white;
            font-weight: 700;
        }
        .sidebar-footer .admin-name {
            font-size: 0.82rem;
            font-weight: 700;
            color: white;
        }
        .sidebar-footer .admin-role {
            font-size: 0.7rem;
            color: rgba(255,255,255,0.4);
        }
        .btn-logout {
            display: flex;
            align-items: center;
            gap: 8px;
            width: 100%;
            padding: 9px 14px;
            background: rgba(229,57,53,0.15);
            border: 1px solid rgba(229,57,53,0.3);
            border-radius: 10px;
            color: #ef9a9a;
            font-size: 0.83rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.2s;
        }
        .btn-logout:hover {
            background: rgba(229,57,53,0.3);
            color: #ffcdd2;
        }

        /* ===== MAIN CONTENT ===== */
        .main-content {
            margin-left: var(--sidebar-width);
            min-height: 100vh;
        }
        .topbar {
            background: white;
            padding: 14px 28px;
            border-bottom: 1px solid #e9ecef;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        }
        .topbar-title {
            font-family: 'Poppins', sans-serif;
            font-size: 1.1rem;
            font-weight: 700;
            color: #2d3748;
        }
        .topbar-right { display: flex; align-items: center; gap: 16px; }
        .topbar-date {
            font-size: 0.8rem;
            color: #90a4ae;
            font-weight: 600;
        }
        .content-area { padding: 28px; }

        /* ===== FLASH MESSAGES ===== */
        .flash-container {
            position: fixed;
            top: 80px; right: 20px;
            z-index: 9999;
            width: 320px;
        }
        .flash-container .alert {
            border-radius: 12px;
            border: none;
            box-shadow: 0 8px 25px rgba(0,0,0,0.12);
            font-size: 0.87rem;
            font-weight: 600;
            animation: slideIn 0.3s ease;
        }
        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to   { transform: translateX(0);    opacity: 1; }
        }

        /* ===== STAT CARDS ===== */
        .stat-card {
            background: white;
            border-radius: 16px;
            padding: 22px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .stat-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }
        .stat-icon {
            width: 52px; height: 52px;
            border-radius: 14px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.4rem;
            margin-bottom: 14px;
        }
        .stat-value {
            font-family: 'Poppins', sans-serif;
            font-size: 2rem;
            font-weight: 700;
            line-height: 1;
            margin-bottom: 4px;
        }
        .stat-label { font-size: 0.82rem; color: #90a4ae; font-weight: 600; }

        /* ===== DATA TABLE ===== */
        .data-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            overflow: hidden;
        }
        .data-card-header {
            padding: 18px 22px;
            border-bottom: 1px solid #f0f4f8;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .data-card-title {
            font-family: 'Poppins', sans-serif;
            font-size: 0.95rem;
            font-weight: 700;
            color: #2d3748;
        }
        .table { margin: 0; }
        .table thead th {
            background: #f8fafc;
            border: none;
            padding: 12px 18px;
            font-size: 0.75rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            color: #90a4ae;
        }
        .table tbody td {
            padding: 14px 18px;
            border-bottom: 1px solid #f0f4f8;
            vertical-align: middle;
            font-size: 0.88rem;
            color: #3d4a5c;
        }
        .table tbody tr:last-child td { border-bottom: none; }
        .table tbody tr:hover { background: #fafcff; }

        /* ===== BADGES ===== */
        .badge-role {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.72rem;
            font-weight: 700;
        }
        .badge-admin        { background: #fce4e4; color: #c62828; }
        .badge-proprietaire { background: #e8f5e9; color: #2e7d32; }
        .badge-gardien      { background: #e3f2fd; color: #1565c0; }
        .badge-active       { background: #e8f5e9; color: #2e7d32; }
        .badge-inactive     { background: #fff3e0; color: #e65100; }
        .badge-deleted      { background: #fce4e4; color: #c62828; }

        /* ===== BOUTONS ACTION ===== */
        .btn-action {
            width: 32px; height: 32px;
            border-radius: 8px;
            border: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
        }
        .btn-view   { background: #e3f2fd; color: #1565c0; }
        .btn-edit   { background: #fff3e0; color: #e65100; }
        .btn-delete { background: #fce4e4; color: #c62828; }
        .btn-restore{ background: #e8f5e9; color: #2e7d32; }
        .btn-action:hover { transform: scale(1.15); }

        /* ===== FORMULAIRES ===== */
        .form-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            padding: 28px;
        }
        .form-section-title {
            font-size: 0.72rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #b0bec5;
            margin-bottom: 16px;
            padding-bottom: 10px;
            border-bottom: 2px solid #f0f4f8;
            display: flex; align-items: center; gap: 8px;
        }
        .form-section-title i { color: var(--primary); }
        .form-label {
            font-size: 0.82rem;
            font-weight: 700;
            color: #546e7a;
            margin-bottom: 6px;
        }
        .form-control, .form-select {
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 10px 14px;
            font-size: 0.9rem;
            transition: border-color 0.2s;
        }
        .form-control:focus, .form-select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(79,121,66,0.12);
        }
        .btn-save {
            background: linear-gradient(135deg, #4f7942, #2d5a27);
            border: none;
            border-radius: 10px;
            padding: 11px 28px;
            font-weight: 700;
            color: white;
            transition: all 0.2s;
        }
        .btn-save:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(79,121,66,0.35);
            color: white;
        }
        .btn-cancel {
            background: #f0f4f8;
            border: none;
            border-radius: 10px;
            padding: 11px 22px;
            font-weight: 700;
            color: #546e7a;
            transition: all 0.2s;
            text-decoration: none;
        }
        .btn-cancel:hover { background: #e2e8f0; color: #2d3748; }

        /* ===== AVATAR ===== */
        .user-avatar {
            width: 36px; height: 36px;
            border-radius: 10px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.85rem;
            color: white;
        }

        /* ===== SEARCH BAR ===== */
        .search-bar {
            position: relative;
        }
        .search-bar input {
            padding-left: 38px;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            font-size: 0.87rem;
        }
        .search-bar input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(79,121,66,0.1);
        }
        .search-bar .search-icon {
            position: absolute;
            left: 12px; top: 50%;
            transform: translateY(-50%);
            color: #b0bec5;
            font-size: 0.85rem;
        }

        /* ===== PAGE HEADER ===== */
        .page-header {
            margin-bottom: 24px;
        }
        .page-header h2 {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
            font-size: 1.5rem;
            color: #2d3748;
            margin-bottom: 4px;
        }
        .page-header p { color: #90a4ae; font-size: 0.87rem; margin: 0; }
        .breadcrumb { background: none; padding: 0; margin: 0; font-size: 0.8rem; }
        .breadcrumb-item a { color: var(--primary); text-decoration: none; font-weight: 600; }
        .breadcrumb-item.active { color: #90a4ae; }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>

{# ===== SIDEBAR ===== #}
<div class="sidebar">
    <div class="sidebar-brand">
        <div class="d-flex align-items-center gap-2 mb-1">
            <span style="font-size:1.5rem;">🐾</span>
            <span class="brand-name">SitMyPet</span>
        </div>
        <div class="brand-sub">Panneau Admin</div>
    </div>

    <nav class="sidebar-nav">
        <div class="sidebar-section">Principal</div>

        <a href="{{ path('app_admin_dashboard') }}"
           class="nav-link {{ app.request.attributes.get('_route') == 'app_admin_dashboard' ? 'active' : '' }}">
            <i class="fas fa-chart-pie"></i> Dashboard
        </a>

        <div class="sidebar-section">Utilisateurs</div>

        <a href="{{ path('app_admin_user_index') }}"
           class="nav-link {{ app.request.attributes.get('_route') starts with 'app_admin_user' ? 'active' : '' }}">
            <i class="fas fa-users"></i> Tous les utilisateurs
        </a>

        <a href="{{ path('app_admin_user_index') }}?role=ROLE_PROPRIETAIRE"
           class="nav-link">
            <i class="fas fa-home"></i> Propriétaires
        </a>

        <a href="{{ path('app_admin_user_index') }}?role=ROLE_GARDIEN"
           class="nav-link">
            <i class="fas fa-hands-helping"></i> Gardiens
        </a>

        <div class="sidebar-section">Application</div>

        <a href="#" class="nav-link">
            <i class="fas fa-dog"></i> Animaux
            <span class="badge-count">Soon</span>
        </a>
        <a href="#" class="nav-link">
            <i class="fas fa-bullhorn"></i> Annonces
            <span class="badge-count">Soon</span>
        </a>
        <a href="#" class="nav-link">
            <i class="fas fa-file-invoice"></i> Factures
            <span class="badge-count">Soon</span>
        </a>
    </nav>

    <div class="sidebar-footer">
        {% if app.user %}
        <div class="admin-info">
            <div class="admin-avatar">
                {{ app.user.prenom|first|upper }}{{ app.user.nom|first|upper }}
            </div>
            <div>
                <div class="admin-name">{{ app.user.prenom }} {{ app.user.nom }}</div>
                <div class="admin-role">Administrateur</div>
            </div>
        </div>
        {% endif %}
        <a href="{{ path('app_logout') }}" class="btn-logout">
            <i class="fas fa-sign-out-alt"></i> Déconnexion
        </a>
    </div>
</div>

{# ===== MAIN CONTENT ===== #}
<div class="main-content">

    {# TOPBAR #}
    <div class="topbar">
        <div class="topbar-title">{% block page_title %}Dashboard{% endblock %}</div>
        <div class="topbar-right">
            <span class="topbar-date">
                <i class="fas fa-calendar me-1"></i>
                {{ "now"|date("d/m/Y") }}
            </span>
            {% if app.user %}
            <div class="d-flex align-items-center gap-2">
                <div style="width:32px;height:32px;background:#4f7942;border-radius:8px;
                            display:flex;align-items:center;justify-content:center;
                            color:white;font-weight:700;font-size:0.8rem;">
                    {{ app.user.prenom|first|upper }}
                </div>
                <span style="font-size:0.85rem;font-weight:700;color:#3d4a5c;">
                    {{ app.user.prenom }}
                </span>
            </div>
            {% endif %}
        </div>
    </div>

    {# FLASH MESSAGES #}
    <div class="flash-container">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i>{{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i>{{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
        {% for message in app.flashes('warning') %}
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i>{{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    </div>

    {# PAGE CONTENT #}
    <div class="content-area">
        {% block content %}{% endblock %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
